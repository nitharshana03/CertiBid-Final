package com.certibid.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
    // Gateway route configurations managed in application.yml
}
